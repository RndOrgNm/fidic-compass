/**
 * Re-export from useRecebiveis for backward compatibility.
 * Recebíveis pipeline replaced prospection workflows.
 */
export {
  useProspectionWorkflows,
  useCreateNewLead,
  useTransitionWorkflow,
  useAssignWorkflow,
} from "@/hooks/useRecebiveis";
